CONCEPT_PROMPT_TEMPLATE = '''
请根据主题 "{topic}" 用中英混合生成{count}个学习该主题必须了解的最重要、最广为人知的相关概念（包括主题本身），可以是主题的子集、子集的子集、应用场景或用法等。
每个概念的名称应同时包含中文和英文信息。
如果这个概念有广泛使用的英文缩写，则格式为：中文名称 (英文全称, 缩写)
如果没有通用的缩写，则格式为：中文名称 (英文全称)
并附上120字左右的中文介绍（以该概念开头），需要尽可能通俗易懂、说人话，适当地使用比喻、类比、举例等方法。

要求输出为合法的 JSON 格式，JSON 的格式为：{{ "概念名称": "概念介绍", ... }}
例如：
{{
    "人工智能 (Artificial Intelligence, AI)": "人工智能是让计算机具备类似人类的智能，能够学习、推理、决策，并执行任务。它通过分析海量数据不断学习成长。手机里的语音助手能听懂你的指令，视频网站会根据喜好推荐节目，自动驾驶汽车能看懂路况——这些都是人工智能在发挥作用。",
    "神经网络 (Neural Network)": "神经网络是模仿人脑神经元构建的智能计算模型，就像快递分拣流水线：第一层拆包检查（输入层），中间团队分类处理（隐藏层），最后主管贴标签（输出层）。每个环节自动优化标准（权重调整），通过反复训练提升准确率。"
    ...
}}
'''

# 修改关系提示模板，使其更明确且包含示例
RELATIONSHIP_PROMPT_TEMPLATE = '''
请根据下面的概念数据构造所有合理且重要的概念间的关联关系（每个概念至少有一个关联关系），每个关联关系以三元组形式呈现。

概念数据为：
{concepts_json}

请返回如下格式的 JSON：
{{
    "relations": [
        ["人工智能 (Artificial Intelligence, AI)", "机器学习 (Machine Learning, ML)", "包含"],
        ["深度学习 (Deep Learning, DL)", "神经网络 (Neural Networks, NN)", "使用"],
        ["深度学习 (Deep Learning, DL)", "自然语言处理 (Natural Language Processing, NLP)", "应用于"]
        ...
    ]
}}

要求：
1. 每个关系必须是一个包含三个元素的数组：[源概念, 目标概念, 关系描述]
2. 关系描述应该简短精确
3. 确保输出是合法的 JSON 格式
4. 只返回 JSON 数据，不要包含其他说明文字
5. 务必确保每个字符串内部的双引号（如有）均已转义。
'''

NEW_CONCEPT_PROMPT_TEMPLATE = '''
请根据用户输入的新概念 "{new_concept_input}" 生成该新概念的详细描述。
要求采用中英混合，格式为：中文名称 (英文全称, 缩写) 或 (英文全称)；附上约120字的中文介绍，通俗易懂、说人话，并适当使用比喻、类比、举例。
要求输出为合法的 JSON 格式，JSON 的格式为：{{ "概念名称": "概念介绍", ... }}
例如：
{{
    "神经网络 (Neural Network)": "神经网络是模仿人脑神经元构建的智能计算模型，就像快递分拣流水线：第一层拆包检查（输入层），中间团队分类处理（隐藏层），最后主管贴标签（输出层）。每个环节自动优化标准（权重调整），通过反复训练提升准确率。"
}}
'''
